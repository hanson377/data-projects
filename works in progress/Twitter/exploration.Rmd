---
title: 'CHeck 1,2,3'
author: 'Josh'
output:
  github_document:
    html_preview: true
---


```{r setup postgresql connection, include = FALSE}
library(dplyr)
library(ggplot2)
library(DescTools)
library(RPostgreSQL)

pg = dbDriver("PostgreSQL")
con = dbConnect(pg, user="postgres", password="Vivamus1431!!",
                 host="localhost", port=5432, dbname="test")

data <- dbGetQuery(con, "SELECT * FROM gop_tweets")
```
